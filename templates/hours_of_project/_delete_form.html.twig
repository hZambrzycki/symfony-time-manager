<form method="post" action="{{ path('app_hours_of_project_delete', {'id': hours_of_project.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hours_of_project.id) }}">
    <button class="btn">Delete</button>
</form>
